diff --git a/Configurations/windows-makefile.tmpl b/Configurations/windows-makefile.tmpl
--- a/Configurations/windows-makefile.tmpl
+++ b/Configurations/windows-makefile.tmpl
@@ -19,6 +19,15 @@
      return map { platform->sharedlib_import($_) // platform->staticlib($_) } @_;
  }
 
+ # BCC64X uses Clang/GNU response-file tokenization even on Windows.
+ # Keep Windows paths for make itself, but optionally use forward slashes
+ # for paths written inside linker response files.
+ sub ld_resp_path {
+     my $path = shift;
+     $path =~ s|\\|/|g if $target{ld_resp_forward_slashes};
+     return $path;
+ }
+
   sub generatetarget {
       my %args = @_;
       my $deps = join(" ", compute_platform_depends(@{$args{deps}}));
@@ -50,13 +59,14 @@ EOF
                 @{$args{objs}};
      my @deps = compute_lib_depends(@{$args{deps}});
      die "More than one exported symbols list" if scalar @defs > 1;
-     my $linklibs = join("", map { "$_$target{ld_resp_delim}" } @deps);
-     my $objs = join($target{ld_resp_delim}, @objs);
-     my $ress = join($target{ld_resp_delim}, @ress);
+     my $linklibs = join("", map { ld_resp_path($_).$target{ld_resp_delim} } @deps);
+     my $objs = join($target{ld_resp_delim}, map { ld_resp_path($_) } @objs);
+     my $ress = join($target{ld_resp_delim}, map { ld_resp_path($_) } @ress);
      my $deps = join(" ", @objs, @ress, @defs, @deps);
      my $import = platform->sharedlib_import($lib);
      my $dll =  platform->sharedlib($lib);
-     my $shared_def = $target{lddefflag} . join("", @defs);
+     my $dll_resp = ld_resp_path($dll);
+     my $shared_def = $target{lddefflag} . join("", map { ld_resp_path($_) } @defs);
      my $implib_rule = $target{ld_implib_rule} || "";
      my $implib_flag = $target{ld_implib_flag}
                        ? "$target{ld_implib_flag}$import"
@@ -71,7 +81,7 @@ $dll: $deps
 	IF EXIST $full.manifest DEL /F /Q $full.manifest
 	IF EXIST \$@ DEL /F /Q \$@
 	cmd /C "\$(LD) \$(LDFLAGS) \$(LIB_LDFLAGS) @<< $implib_flag || (DEL /Q \$(\@B).* $import & EXIT 1)"
-$objs$target{ld_resp_delim}\$(LDOUTFLAG)$dll$target{ldpostoutflag}$target{ld_resp_delim}$linklibs\$(LIB_EX_LIBS)$target{ld_resp_delim}$shared_def$target{ldresflag}$ress
+$objs$target{ld_resp_delim}\$(LDOUTFLAG)$dll_resp$target{ldpostoutflag}$target{ld_resp_delim}$linklibs\$(LIB_EX_LIBS)$target{ld_resp_delim}$shared_def$target{ldresflag}$ress
 <<
 	IF EXIST $dll.manifest \\
 	   \$(MT) \$(MTFLAGS) \$(MTINFLAG)$dll.manifest \$(MTOUTFLAG)$dll
@@ -102,16 +112,17 @@ EOF
                 @{$args{objs}};
      my @deps = compute_lib_depends(@{$args{deps}});
      die "More than one exported symbols list" if scalar @defs > 1;
-     my $objs = join($target{ld_resp_delim}, @objs);
-     my $ress = join($target{ld_resp_delim}, @ress);
-     my $linklibs = join("", map { "$_$target{ld_resp_delim}" } @deps);
+     my $objs = join($target{ld_resp_delim}, map { ld_resp_path($_) } @objs);
+     my $ress = join($target{ld_resp_delim}, map { ld_resp_path($_) } @ress);
+     my $linklibs = join("", map { ld_resp_path($_).$target{ld_resp_delim} } @deps);
      my $deps = join(" ", @objs, @ress, @defs, @deps);
-     my $shared_def = $target{lddefflag} . join("", @defs);
+     my $dso_resp = ld_resp_path($dso);
+     my $shared_def = $target{lddefflag} . join("", map { ld_resp_path($_) } @defs);
      return <<"EOF";
 $dso: $deps
 	IF EXIST $dso.manifest DEL /F /Q $dso.manifest
 	cmd /C "\$(LD) \$(LDFLAGS) \$(DSO_LDFLAGS) @<< || (DEL /Q \$(\@B).* $dso_n.* & EXIT 1)"
-$objs$target{ld_resp_delim}\$(LDOUTFLAG)$dso$target{ldpostoutflag}$target{ld_resp_delim}$linklibs \$(DSO_EX_LIBS)$target{ld_resp_delim}$shared_def$target{ldresflag}$ress
+$objs$target{ld_resp_delim}\$(LDOUTFLAG)$dso_resp$target{ldpostoutflag}$target{ld_resp_delim}$linklibs \$(DSO_EX_LIBS)$target{ld_resp_delim}$shared_def$target{ldresflag}$ress
 <<
 	IF EXIST $dso.manifest \\
 	   \$(MT) \$(MTFLAGS) \$(MTINFLAG)$dso.manifest \$(MTOUTFLAG)$dso
@@ -137,15 +148,16 @@ EOF
                 grep { platform->isres($_) }
                 @{$args{objs}};
      my @deps = compute_lib_depends(@{$args{deps}});
-     my $objs = join($target{ld_resp_delim}, @objs);
-     my $ress = join($target{ld_resp_delim}, @ress);
-     my $linklibs = join("", map { "$_$target{ld_resp_delim}" } @deps);
+     my $objs = join($target{ld_resp_delim}, map { ld_resp_path($_) } @objs);
+     my $ress = join($target{ld_resp_delim}, map { ld_resp_path($_) } @ress);
+     my $linklibs = join("", map { ld_resp_path($_).$target{ld_resp_delim} } @deps);
      my $deps = join(" ", @objs, @ress, @deps);
+     my $bin_resp = ld_resp_path($bin);
      return <<"EOF";
 $bin: $deps
 	IF EXIST $bin.manifest DEL /F /Q $bin.manifest
 	\$(LD) \$(LDFLAGS) \$(BIN_LDFLAGS) @<<
-$objs$target{ld_resp_delim}\$(LDOUTFLAG)$bin$target{ldpostoutflag}$target{ld_resp_delim}$linklibs\$(BIN_EX_LIBS)$target{ldresflag}$target{ldresflag}$ress
+$objs$target{ld_resp_delim}\$(LDOUTFLAG)$bin_resp$target{ldpostoutflag}$target{ld_resp_delim}$linklibs\$(BIN_EX_LIBS)$target{ldresflag}$target{ldresflag}$ress
 <<
 	IF EXIST $bin.manifest \\
 	   \$(MT) \$(MTFLAGS) \$(MTINFLAG)$bin.manifest \$(MTOUTFLAG)$bin
@@ -268,7 +280,8 @@
 CFLAGS={- join(' ', @{$config{CFLAGS}}) -}
 LD="{- $config{LD} -}"
 LDFLAGS={- join(' ', @{$config{LDFLAGS}}) -}
 EX_LIBS={- join(' ', @{$config{LDLIBS}}) -}
+EX_LIBS_RESP={- join(' ', map { ld_resp_path($_) } @{$config{LDLIBS}}) -}
 
 PERL={- $config{PERL} -}
 
@@ -316,8 +329,10 @@
 CNF_CXXFLAGS={- join(' ', $target{cxxflags} || (),
                           @{$config{cxxflags}}) -}
 CNF_LDFLAGS={- join(' ', $target{lflags} || (),
                          @{$config{lflags}}) -}
 CNF_EX_LIBS={- join(' ', $target{ex_libs} || (),
                          @{$config{ex_libs}}) -}
+CNF_EX_LIBS_RESP={- join(' ', map { ld_resp_path($_) }
+                              ($target{ex_libs} || (), @{$config{ex_libs}})) -}
 
 # Variables starting with LIB_ are used to build library object files
@@ -357,7 +372,7 @@
 LIB_LDFLAGS={- join(' ', $target{shared_ldflag} || (),
                          $config{shared_ldflag} || (),
                          '$(CNF_LDFLAGS)', '$(LDFLAGS)') -}
-LIB_EX_LIBS=$(CNF_EX_LIBS) $(EX_LIBS)
+LIB_EX_LIBS=$(CNF_EX_LIBS_RESP) $(EX_LIBS_RESP)
 DSO_ASFLAGS={- join(' ', $target{dso_asflags} || (),
                          $target{module_asflags} || (),
                          @{$config{dso_asflags}},
@@ -386,8 +401,8 @@
 DSO_LDFLAGS={- join(' ', $target{dso_lflags} || (),
                          $target{module_ldflags} || (),
                          @{$config{dso_lflags}},
                          @{$config{module_ldflags}},
                          '$(CNF_LDFLAGS)', '$(LDFLAGS)') -}
-DSO_EX_LIBS=$(CNF_EX_LIBS) $(EX_LIBS)
+DSO_EX_LIBS=$(CNF_EX_LIBS_RESP) $(EX_LIBS_RESP)
 BIN_ASFLAGS={- join(' ', $target{bin_asflags} || (),
                          @{$config{bin_asflags}},
@@ -402,4 +417,4 @@
 BIN_LDFLAGS={- join(' ', $target{bin_lflags} || (),
                          @{$config{bin_lflags}},
                          '$(CNF_LDFLAGS)', '$(LDFLAGS)') -}
-BIN_EX_LIBS=$(CNF_EX_LIBS) $(EX_LIBS)
+BIN_EX_LIBS=$(CNF_EX_LIBS_RESP) $(EX_LIBS_RESP)
