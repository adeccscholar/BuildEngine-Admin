diff --git a/src/podofo/private/XMPUtils.cpp b/src/podofo/private/XMPUtils.cpp
--- a/src/podofo/private/XMPUtils.cpp
+++ b/src/podofo/private/XMPUtils.cpp
@@ -1273,7 +1273,15 @@
         }
         ~Init()
         {
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-begin PDFA1 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
             xmlRelaxNGFree(m_schema);
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-end PDFA1 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
         }
         xmlRelaxNGPtr m_schema;
     } s_init;
@@ -1299,7 +1307,15 @@
         }
         ~Init()
         {
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-begin PDFA2_3 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
             xmlRelaxNGFree(m_schema);
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-end PDFA2_3 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
         }
         xmlRelaxNGPtr m_schema;
     } s_init;
@@ -1327,7 +1343,15 @@
         }
         ~Init()
         {
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-begin PDFA4 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
             xmlRelaxNGFree(m_schema);
+#if defined(__clang__) && defined(__BORLANDC__)
+            std::fprintf(stderr, "PODOFO_XMP_SCHEMA destroy-end PDFA4 schema=%p\n", (void*)m_schema);
+            std::fflush(stderr);
+#endif
         }
         xmlRelaxNGPtr m_schema;
     } s_init;
