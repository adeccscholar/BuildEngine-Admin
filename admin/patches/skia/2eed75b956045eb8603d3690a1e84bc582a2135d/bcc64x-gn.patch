diff --git a/BUILD.gn b/BUILD.gn
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -171,7 +171,7 @@ is_loong64 = current_cpu == "loong64"
 opts("ml3") {
   enabled = is_x86
   sources = skia_opts.ml3_sources
-  if (is_win) {
+  if (is_win && !skia_use_bcc64x) {
     cflags = [ "/arch:AVX2" ]
   } else if (current_cpu == "x86") {
     cflags = [ "-march=haswell" ]
@@ -183,7 +183,7 @@ opts("ml3") {
 opts("ml4") {
   enabled = is_x86
   sources = skia_opts.ml4_sources
-  if (is_win) {
+  if (is_win && !skia_use_bcc64x) {
     cflags = [ "/arch:AVX512" ]
   } else if (current_cpu == "x86") {
     cflags = [ "-march=skylake-avx512" ]
diff --git a/gn/BUILDCONFIG.gn b/gn/BUILDCONFIG.gn
--- a/gn/BUILDCONFIG.gn
+++ b/gn/BUILDCONFIG.gn
@@ -189,6 +189,10 @@ template("component") {
   }
 }
 
+declare_args() {
+  skia_use_bcc64x = false
+}
+
 # Default configs
 default_configs = [
   "//gn/skia:default",
@@ -247,7 +251,13 @@ if (!is_official_build) {
       [ "//gn/skia:warnings_for_public_headers" ]
 }
 
-if (is_win) {
+if (is_win && skia_use_bcc64x) {
+  # BCC64X is the Windows target compiler. Skia host tools remain on the
+  # native Windows host toolchain.
+  set_default_toolchain("//gn/toolchain:gcc_like")
+  default_toolchain_name = "gcc_like"
+  host_toolchain = "msvc_host"
+} else if (is_win) {
   # Windows tool chain
   set_default_toolchain("//gn/toolchain:msvc")
   default_toolchain_name = "msvc"
diff --git a/gn/skia/BUILD.gn b/gn/skia/BUILD.gn
--- a/gn/skia/BUILD.gn
+++ b/gn/skia/BUILD.gn
@@ -9,6 +9,8 @@ if (is_wasm) {
 
 import("../skia.gni")
 
+is_bcc64x_toolchain = skia_use_bcc64x && current_toolchain == default_toolchain
+
 declare_args() {
   extra_asmflags = []
   extra_cflags = []
@@ -59,7 +61,7 @@ config("default") {
   # Disable warnings about unknown attributes.
   # (These unknown attribute warnings are on by default, so we don't make
   # disabling them part of :warnings, as some targets remove :warnings.)
-  if (is_win && !is_clang) {
+  if (is_win && !is_clang && !is_bcc64x_toolchain) {
     cflags += [
       "/wd5030",
       "/wd5051",
@@ -74,7 +76,7 @@ config("default") {
     cflags += [ "-ffp-contract=off" ]
   }
 
-  if (is_win) {
+  if (is_win && !is_bcc64x_toolchain) {
     if (is_clang && current_cpu == "arm64") {
       cflags += [ "--target=arm64-windows" ]
     }
@@ -409,7 +411,7 @@ config("recover_pointer_overflow") {
 
 config("no_exceptions") {
   # Exceptions are disabled by default on Windows.  (Use /EHsc to enable them.)
-  if (!is_win) {
+  if (!is_win || is_bcc64x_toolchain) {
     cflags_cc = [ "-fno-exceptions" ]
     cflags_objcc = cflags_cc
   }
@@ -422,14 +424,14 @@ config("warnings") {
   cflags_objcc = []
 
   if (werror) {
-    if (is_win) {
+    if (is_win && !is_bcc64x_toolchain) {
       cflags += [ "/WX" ]
     } else {
       cflags += [ "-Werror" ]
     }
   }
 
-  if (is_win) {
+  if (is_win && !is_bcc64x_toolchain) {
     cflags += [
       "/W3",  # Turn on lots of warnings.
 
@@ -684,6 +686,8 @@ config("debug_symbols") {
       "-gline-tables-only",
       "-funwind-tables",  # Helps make in-process backtraces fuller.
     ]
+  } else if (is_win && is_bcc64x_toolchain) {
+    cflags = [ "-g" ]
   } else if (is_win) {
     cflags = [ "/Z7" ]
     if (is_clang) {
@@ -704,7 +708,7 @@ config("no_rtti") {
   # -fsanitize=vptr requires RTTI which is enabled as a part of
   # -fsanitize=undefined
   if (sanitize != "ASAN") {
-    if (is_win) {
+    if (is_win && !is_bcc64x_toolchain) {
       cflags_cc = [ "/GR-" ]
     } else {
       cflags_cc = [ "-fno-rtti" ]
@@ -715,7 +719,7 @@ config("no_rtti") {
 
 config("optimize") {
   ldflags = []
-  if (is_win) {
+  if (is_win && !is_bcc64x_toolchain) {
     cflags = [
       "/O2",
       "/Zc:inline",
diff --git a/gn/toolchain/BUILD.gn b/gn/toolchain/BUILD.gn
--- a/gn/toolchain/BUILD.gn
+++ b/gn/toolchain/BUILD.gn
@@ -6,6 +6,9 @@ declare_args() {
   host_ar = ar
   host_cc = cc
   host_cxx = cxx
+  python_executable = "python3"
+  bcc64x_rad_lib_dir = ""
+  bcc64x_rad_psdk_lib_dir = ""
 
   if (is_android) {
     _prefix = "$ndk/toolchains/llvm/prebuilt/$ndk_host/bin"
@@ -219,7 +222,7 @@ declare_args() {
 
     tool("copy") {
       cp_py = rebase_path("../cp.py")
-      command = "$shell python3 \"$cp_py\" {{source}} {{output}}"
+      command = "$shell $python_executable \"$cp_py\" {{source}} {{output}}"
       description = "copy {{source}} {{output}}"
     }
 
@@ -248,6 +251,10 @@ template("gcc_like_toolchain") {
     link = invoker.link
     lib_switch = "-l"
     lib_dir_switch = "-L"
+    if (current_os == "win" && skia_use_bcc64x &&
+        current_toolchain == default_toolchain) {
+      lib_switch = ""
+    }
 
     current_cpu = invoker.cpu
     current_os = invoker.os
@@ -312,7 +319,7 @@ template("gcc_like_toolchain") {
         rspfile = "{{output}}.rsp"
         rspfile_content = "{{inputs}}"
         rm_py = rebase_path("../rm.py")
-        command = "$shell python3 \"$rm_py\" \"{{output}}\" && $ar rcs {{output}} @$rspfile"
+        command = "$shell $python_executable \"$rm_py\" \"{{output}}\" && $ar rcs {{output}} @$rspfile"
       }
 
       outputs =
@@ -330,15 +337,37 @@ template("gcc_like_toolchain") {
     }
 
     tool("solink") {
-      soname = "{{target_output_name}}{{output_extension}}"
+      if (current_os == "win" && skia_use_bcc64x &&
+          current_toolchain == default_toolchain) {
+        dllname = "{{root_out_dir}}/{{target_output_name}}{{output_extension}}"
+        libname = "{{root_out_dir}}/{{target_output_name}}.lib"
+        rspfile = "${dllname}.rsp"
+        command = "$shell $link --rsp-quoting=windows -L\"$bcc64x_rad_lib_dir\" -L\"$bcc64x_rad_psdk_lib_dir\" -tD -o$dllname @$rspfile -Wl,--out-implib,$libname"
+        rspfile_content = "{{inputs}} {{libs}} {{solibs}} {{ldflags}}"
+        outputs = [
+          dllname,
+          libname,
+        ]
+        default_output_extension = ".dll"
+        default_output_dir = "{{root_out_dir}}"
+        link_output = libname
+        depend_output = libname
+        runtime_outputs = [ dllname ]
+        restat = true
+        description = "link {{output}}"
+        if (0 <= link_pool_depth) {
+          pool = ":link_pool($default_toolchain)"
+        }
+      } else {
+        soname = "{{target_output_name}}{{output_extension}}"
 
-      rpath = "-Wl,-soname,$soname"
-      if (current_os == "mac" || current_os == "ios" || current_os == "tvos") {
-        rpath = "-Wl,-install_name,@rpath/$soname"
-      }
+        rpath = "-Wl,-soname,$soname"
+        if (current_os == "mac" || current_os == "ios" || current_os == "tvos") {
+          rpath = "-Wl,-install_name,@rpath/$soname"
+        }
 
-      rspfile = "{{output}}.rsp"
-      rspfile_content = "{{inputs}}"
+        rspfile = "{{output}}.rsp"
+        rspfile_content = "{{inputs}}"
 
       # --start-group/--end-group let us link multiple .a {{inputs}}
       # without worrying about their relative order on the link line.
@@ -358,19 +387,20 @@ template("gcc_like_toolchain") {
         _emcc_flags = "-sSIDE_MODULE=1"
       }
 
-      command = "$link -shared {{ldflags}} $_start_group @$rspfile {{frameworks}} {{solibs}} $_end_group {{libs}} $rpath $_emcc_flags -o {{output}}"
-      outputs = [ "{{root_out_dir}}/$soname" ]
-      output_prefix = "lib"
-      if (current_os == "mac" || current_os == "ios" || current_os == "tvos") {
-        default_output_extension = ".dylib"
-      } else if (current_os == "wasm" && !is_canvaskit) {
-        default_output_extension = ".wasm.so"
-      } else {
-        default_output_extension = ".so"
-      }
-      description = "link {{output}}"
-      if (0 <= link_pool_depth) {
-        pool = ":link_pool($default_toolchain)"
+        command = "$link -shared {{ldflags}} $_start_group @$rspfile {{frameworks}} {{solibs}} $_end_group {{libs}} $rpath $_emcc_flags -o {{output}}"
+        outputs = [ "{{root_out_dir}}/$soname" ]
+        output_prefix = "lib"
+        if (current_os == "mac" || current_os == "ios" || current_os == "tvos") {
+          default_output_extension = ".dylib"
+        } else if (current_os == "wasm" && !is_canvaskit) {
+          default_output_extension = ".wasm.so"
+        } else {
+          default_output_extension = ".so"
+        }
+        description = "link {{output}}"
+        if (0 <= link_pool_depth) {
+          pool = ":link_pool($default_toolchain)"
+        }
       }
     }
 
@@ -413,13 +443,13 @@ template("gcc_like_toolchain") {
 
     tool("copy") {
       cp_py = rebase_path("../cp.py")
-      command = "python3 \"$cp_py\" {{source}} {{output}}"
+      command = "$python_executable \"$cp_py\" {{source}} {{output}}"
       description = "copy {{source}} {{output}}"
     }
 
     tool("copy_bundle_data") {
       cp_py = rebase_path("../cp.py")
-      command = "python3 \"$cp_py\" {{source}} {{output}}"
+      command = "$python_executable \"$cp_py\" {{source}} {{output}}"
       description = "copy_bundle_data {{source}} {{output}}"
     }
 
diff --git a/modules/skcms/BUILD.gn b/modules/skcms/BUILD.gn
--- a/modules/skcms/BUILD.gn
+++ b/modules/skcms/BUILD.gn
@@ -34,7 +34,13 @@ arch("skcms_TransformHsw") {
   enabled = current_cpu == "x64" && target_os != "android"
   sources = skcms_TransformHsw
   if (is_win) {
-    if (is_clang) {
+    if (skia_use_bcc64x) {
+      cflags = [
+        "-mavx2",
+        "-mf16c",
+        "-ffp-contract=off",
+      ]
+    } else if (is_clang) {
       cflags = [
         "/clang:-mavx2",
         "/clang:-mf16c",
@@ -56,7 +62,16 @@ arch("skcms_TransformSkx") {
   enabled = current_cpu == "x64" && target_os != "android"
   sources = skcms_TransformSkx
   if (is_win) {
-    if (is_clang) {
+    if (skia_use_bcc64x) {
+      cflags = [
+        "-mavx512f",
+        "-mavx512dq",
+        "-mavx512cd",
+        "-mavx512bw",
+        "-mavx512vl",
+        "-ffp-contract=off",
+      ]
+    } else if (is_clang) {
       cflags = [
         "/clang:-mavx512f",
         "/clang:-mf16c",
diff --git a/src/utils/win/SkDWriteFontFileStream.h b/src/utils/win/SkDWriteFontFileStream.h
--- a/src/utils/win/SkDWriteFontFileStream.h
+++ b/src/utils/win/SkDWriteFontFileStream.h
@@ -81,7 +81,7 @@ private:
     explicit SkDWriteFontFileStreamWrapper(SkStreamAsset* stream);
     virtual ~SkDWriteFontFileStreamWrapper() { }
 
-    ULONG fRefCount;
+    LONG fRefCount;
     std::unique_ptr<SkStreamAsset> fStream;
     SkMutex fStreamMutex;
 };
diff --git a/third_party/third_party.gni b/third_party/third_party.gni
--- a/third_party/third_party.gni
+++ b/third_party/third_party.gni
@@ -92,7 +92,9 @@
       if (!defined(cflags)) {
         cflags = []
       }
-      if (is_win) {
+      if (is_win && skia_use_bcc64x && current_toolchain == default_toolchain) {
+        cflags += [ "-w" ]
+      } else if (is_win) {
         cflags += [ "/w" ]
       } else {
         cflags += [ "-w" ]
diff --git a/BUILD.gn b/BUILD.gn
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -2165,16 +2165,31 @@
                  "src/ports/SkImageGeneratorWIC.cpp",
                  "src/ports/SkLog_win.cpp",
                ]
-    libs += [
-      "Ole32.lib",
-      "OleAut32.lib",
-    ]
+    if (skia_use_bcc64x) {
+      libs += [
+        "Ole32",
+        "OleAut32",
+      ]
+    } else {
+      libs += [
+        "Ole32.lib",
+        "OleAut32.lib",
+      ]
+    }
 
     if (!skia_enable_winuwp) {
-      libs += [
-        "FontSub.lib",
-        "User32.lib",
-        "Usp10.lib",
-      ]
+      if (skia_use_bcc64x) {
+        libs += [
+          "FontSub",
+          "User32",
+          "Usp10",
+        ]
+      } else {
+        libs += [
+          "FontSub.lib",
+          "User32.lib",
+          "Usp10.lib",
+        ]
+      }
     }
   } else {
diff --git a/BUILD.gn b/BUILD.gn
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -503,7 +503,11 @@
   public_defines = [ "SK_FONTMGR_GDI_AVAILABLE" ]
   public = skia_ports_windows_fonts_public
   sources = skia_ports_fonthost_win_sources
-  libs = [ "Gdi32.lib" ]
+  if (skia_use_bcc64x) {
+    libs = [ "Gdi32" ]
+  } else {
+    libs = [ "Gdi32.lib" ]
+  }
 }
 
 if (skia_lex) {
diff --git a/gn/toolchain/BUILD.gn b/gn/toolchain/BUILD.gn
--- a/gn/toolchain/BUILD.gn
+++ b/gn/toolchain/BUILD.gn
@@ -342,5 +342,5 @@
         dllname = "{{root_out_dir}}/{{target_output_name}}{{output_extension}}"
         libname = "{{root_out_dir}}/{{target_output_name}}.lib"
         rspfile = "${dllname}.rsp"
-        command = "$shell $link --rsp-quoting=windows -L\"$bcc64x_rad_lib_dir\" -L\"$bcc64x_rad_psdk_lib_dir\" -tD -o$dllname @$rspfile -Wl,--out-implib,$libname"
+        command = "$shell $link --rsp-quoting=windows -L\"$bcc64x_mingw_lib_dir\" -tD -o$dllname @$rspfile -Wl,--out-implib,$libname"
         rspfile_content = "{{inputs}} {{libs}} {{solibs}} {{ldflags}}"
diff --git a/gn/toolchain/BUILD.gn b/gn/toolchain/BUILD.gn
--- a/gn/toolchain/BUILD.gn
+++ b/gn/toolchain/BUILD.gn
@@ -251,10 +251,6 @@
     link = invoker.link
     lib_switch = "-l"
     lib_dir_switch = "-L"
-    if (current_os == "win" && skia_use_bcc64x &&
-        current_toolchain == default_toolchain) {
-      lib_switch = ""
-    }
 
     current_cpu = invoker.cpu
     current_os = invoker.os
diff --git a/gn/toolchain/BUILD.gn b/gn/toolchain/BUILD.gn
--- a/gn/toolchain/BUILD.gn
+++ b/gn/toolchain/BUILD.gn
@@ -7,7 +7,8 @@
   host_cc = cc
   host_cxx = cxx
   python_executable = "python3"
   bcc64x_rad_lib_dir = ""
   bcc64x_rad_psdk_lib_dir = ""
+  bcc64x_mingw_lib_dir = getenv("CB_BDS") + "/x86_64-w64-mingw32/lib"
 
   if (is_android) {
diff --git a/gn/skia/BUILD.gn b/gn/skia/BUILD.gn
--- a/gn/skia/BUILD.gn
+++ b/gn/skia/BUILD.gn
@@ -477,10 +477,12 @@
       "-Wvla",
 
       "-Wno-deprecated-declarations",
-      "-Wno-maybe-uninitialized",
       "-Wno-psabi",
       "-Wno-switch-default",  # Warns even when all enum values are covered
       "-Wno-unused-parameter",  # Re-enabled for header sources
     ]
+    if (!is_bcc64x_toolchain) {
+      cflags += [ "-Wno-maybe-uninitialized" ]
+    }
     cflags_cc += [
       "-Wnon-virtual-dtor",
diff --git a/include/private/SkFloatingPoint.h b/include/private/SkFloatingPoint.h
--- a/include/private/SkFloatingPoint.h
+++ b/include/private/SkFloatingPoint.h
@@ -153,6 +153,6 @@
 // IEEE defines how float divide behaves for non-finite values and zero-denoms, but C does not,
 // so we have a helper that suppresses the possible undefined-behavior warnings.
-#ifdef SK_BUILD_FOR_WIN
+#if defined(SK_BUILD_FOR_WIN) && !(defined(__BORLANDC__) && defined(__clang__))
 #pragma warning(push)
 #pragma warning(disable : 4723)
 #endif
@@ -165,7 +165,7 @@
 static constexpr double sk_ieee_double_divide(double numer, double denom) {
     return numer / denom;
 }
-#ifdef SK_BUILD_FOR_WIN
+#if defined(SK_BUILD_FOR_WIN) && !(defined(__BORLANDC__) && defined(__clang__))
 #pragma warning( pop )
 #endif
 
