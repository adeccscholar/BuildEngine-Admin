diff --git a/third_party/zlib/BUILD.gn b/third_party/zlib/BUILD.gn
--- a/third_party/zlib/BUILD.gn
+++ b/third_party/zlib/BUILD.gn
@@ -9,6 +9,10 @@ import("zlib.gni")
 if (skia_use_system_zlib) {
   system("zlib") {
     if (skia_use_bcc64x) {
+      assert(bcc64x_zlib_include != "",
+             "bcc64x_zlib_include must name the BuildEngine zlib include directory")
+      assert(bcc64x_zlib_lib != "",
+             "bcc64x_zlib_lib must name the BuildEngine zlib import library")
       include_dirs = [ bcc64x_zlib_include ]
       ldflags = [ bcc64x_zlib_lib ]
     } else {
