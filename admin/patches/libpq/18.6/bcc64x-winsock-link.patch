diff --git a/src/interfaces/libpq/meson.build b/src/interfaces/libpq/meson.build
--- a/src/interfaces/libpq/meson.build
+++ b/src/interfaces/libpq/meson.build
@@ -17,6 +17,7 @@ libpq_sources = files(
   'legacy-pqsignal.c',
   'libpq-events.c',
   'pqexpbuffer.c',
+  'bcc64x-winsock-compat.c',
 )
 libpq_so_sources = [] # for shared lib, in addition to the above
 
diff --git a/src/interfaces/libpq/bcc64x-winsock-compat.c b/src/interfaces/libpq/bcc64x-winsock-compat.c
new file mode 100644
--- /dev/null
+++ b/src/interfaces/libpq/bcc64x-winsock-compat.c
@@ -0,0 +1,24 @@
+/*
+ * BCC64X compatibility for Windows socket symbols that are declared by the
+ * selected headers but not emitted/provided with the linkage expected here.
+ */
+
+#if defined(__clang__) && defined(__BORLANDC__)
+
+#include <winsock2.h>
+
+const IN_ADDR in4addr_any = { { IN4ADDR_ANY_INIT } };
+
+BOOLEAN
+IN6_IS_ADDR_V4TRANSLATED(const IN6_ADDR *a)
+{
+    return (BOOLEAN) ((a->s6_words[0] == 0) &&
+                      (a->s6_words[1] == 0) &&
+                      (a->s6_words[2] == 0) &&
+                      (a->s6_words[3] == 0) &&
+                      (a->s6_words[4] == 0xffff) &&
+                      (a->s6_words[5] == 0));
+}
+
+#endif
+