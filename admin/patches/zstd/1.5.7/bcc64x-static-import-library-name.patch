diff --git a/build/cmake/lib/CMakeLists.txt b/build/cmake/lib/CMakeLists.txt
--- a/build/cmake/lib/CMakeLists.txt
+++ b/build/cmake/lib/CMakeLists.txt
@@ -177,7 +177,10 @@ if (MSVC)
 endif ()
 
 # With MSVC static library needs to be renamed to avoid conflict with import library
-if (MSVC OR (WIN32 AND CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND NOT MINGW))
+if (MSVC OR (WIN32 AND NOT MINGW AND
+             (CMAKE_CXX_COMPILER_ID STREQUAL "Clang" OR
+              CMAKE_CXX_COMPILER_ID STREQUAL "Embarcadero" OR
+              CMAKE_CXX_COMPILER_ID STREQUAL "Borland")))
     set(STATIC_LIBRARY_BASE_NAME zstd_static)
 else ()
     set(STATIC_LIBRARY_BASE_NAME zstd)
